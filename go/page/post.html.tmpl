{{- /*gotype:github.com/blazejsewera/blog/page.PostProps*/ -}}
{{ block "Post" . }}
    <!doctype html>
    <html lang="{{ .Header.Language }}">
    <head>
        {{ template "Header" .Header }}
        {{ template "ArticleStyle" .Article }}
        <title>{{ .Header.Title }}</title>
    </head>
    <body>
    {{ template "Article" .Article }}
    </body>
    </html>
{{ end }}
